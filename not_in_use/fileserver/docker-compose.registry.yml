version: "3"
services:
  registry:
      restart: on-failure
      image: registry:2
      environment:
        - REGISTRY_STORAGE_DELETE_ENABLED=true
      ports:
        - 5000:5000
      volumes:
        - /mnt/docker-registry:/var/lib/registry
      healthcheck:
        interval: 1h


timeout: 3600s
steps:
  - name: gcr.io/worpdress-279321/videosdb-builder
    env:
        - 'BUILD_ID=$BUILD_ID'
        - 'VIDEOSDB_CONFIG=$_VIDEOSDB_CONFIG'
        - 'LOGLEVEL=$_LOGLEVEL'
    args:
        - --verbose
        - $TRIGGER_NAME

